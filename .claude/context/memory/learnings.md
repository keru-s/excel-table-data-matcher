- 2026-03-11: Windows 打包版 PyQt6 工具避免在 GUI 线程里读 Excel/CSV；预览和导出都应放到后台线程。
- 2026-03-11: 对大文件导出，优先使用单后台线程配合流式读取和 write-only Excel 写出，避免 Windows `spawn` 多进程带来的卡死和高内存复制。
- 2026-03-11: 主题切换优先提供“跟随系统/浅色/深色”三档，并监听 Qt 调色板或 `colorScheme` 变化实现自动暗色切换。
- 2026-03-11: 当用户未显式指定工作表时，若首个 sheet 为空，应自动切换到第一个有表头且至少一行数据的工作表。
- 2026-03-11: 导出匹配结果默认按当前匹配列唯一化，两侧文件都只保留每个匹配键的第一条记录，避免高重复键触发笛卡尔积爆炸。
- 2026-03-11: 去重策略需要可配置；默认策略应为“左侧按匹配列去重，右侧仅对同键下的完全重复行去重”，同时保留“关闭去重”和“两侧都按匹配列去重”两档。
- 2026-03-12: 这类桌面数据工具界面优先用 2x2 等宽网格保持四块卡片上下左右对齐；长文本策略选项应拆进独立设置面板，不要塞进单行表单格。
- 2026-03-12: 输出设置区里“输出列”是主内容，编码和去重策略属于次级设置，应缩成底部紧凑工具条；默认窗口尺寸也要足够大，避免首次打开就压坏控件。
- 2026-03-12: 默认空态不能让文件预览和匹配规则占大块空白；未加载文件时应显示紧凑占位态，并把更多默认高度留给输出设置。
